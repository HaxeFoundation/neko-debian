Description: Fix FTBFS with ld --as-needed. LP: #771060.
Author: Matthias Klose <doko@ubuntu.com>
Bug-Ubuntu: https://bugs.launchpad.net/bugs/771060

--- neko-1.8.1.orig/src/tools/install.neko
+++ neko-1.8.1/src/tools/install.neko
@@ -253,9 +253,9 @@ link = function(files,target,params1,par
 		dir[0] + ".o"
 	}));
 	var opt = linkoptions;
-	var cmd = linkcmd+" "+opt+" "+make("-L",libraries);
+	var cmd = linkcmd+" "+make("-L",libraries);
 	var out = " -o ../../bin/"+target+".ndll ";
-	exec(cmd+out+params1+files+params2);
+	exec(cmd+out+files+params1+params2+" "+opt);
 }
 
 // COMPILATION
